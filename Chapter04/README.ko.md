# Chapter 04: 파일 읽고 쓰기

[English](./README.md) | **한국어**

## 이 챕터에서 배우는 것

- Claude에게 파일 참조하는 법 (@-mention)
- 파일 생성 및 수정
- 여러 파일 동시 처리

---

## 왜 필요합니까?

코딩에서 파일은 전부입니다. 웹사이트, 앱, 도구 등 만드는 모든 것이 파일로 이루어져 있습니다. 파일 다루는 법을 배우는 건 요리에서 재료 다루는 법을 배우는 것과 같습니다--재료 없이는 아무것도 만들 수 없습니다.

**실제 상황:**
- 웹사이트를 만들고 싶다 (HTML, CSS 파일 필요)
- 기존 문서를 수정하고 싶다
- 어지러운 프로젝트 파일을 정리하고 싶다
- Claude가 프로젝트 구조를 이해하게 하고 싶다

### 쉬운 비유: 서류 캐비닛

컴퓨터를 거대한 서류 캐비닛이라고 생각해보십시오. 각 서랍이 폴더이고, 안에 있는 각 종이가 파일입니다.

`@report.txt`라고 말하면, 특정 종이를 가리키면서 "이거 봐"라고 하는 겁니다. `@documents/`라고 말하면, 서랍 전체를 가리키면서 "여기 있는 거 다 확인해"라고 하는 겁니다.

`@` 기호는 Claude가 볼 것을 가리키는 여러분의 손가락입니다.

---

## @로 파일 참조하기

Claude Code에서 `@`를 사용하면 파일을 참조할 수 있습니다.

### 기본 사용법

```
> @index.html 이 파일 설명해줘
```

```
> @src/app.js 이 코드에서 버그 찾아줘
```

```
> @package.json 여기서 사용하는 라이브러리 목록 알려줘
```

### 자동완성

`@`를 입력하고 파일 이름을 입력하면 자동으로 목록이 나타납니다. 화살표로 선택하고 Enter를 누릅니다.

### 폴더 참조

```
> @src/ 이 폴더 구조 설명해줘
```

```
> @components/ 여기 있는 파일들 뭐하는 거야?
```

---

## 파일 생성

Claude에게 파일을 생성해달라고 요청합니다.

### 예시

```
> hello.txt 파일 만들어줘. 안에 "안녕하세요"라고 써줘.
```

```
> index.html 파일 만들어줘. 간단한 웹페이지로.
```

### 승인 절차

Normal 모드에서는 Claude가 파일을 생성하기 전에 확인을 요청합니다:

```
Claude wants to create hello.txt
---
안녕하세요
---
[Allow] [Deny]
```

`Allow`를 선택하면 파일이 생성됩니다.

### 폴더와 함께 생성

```
> src/components/Button.js 파일 만들어줘. React 버튼 컴포넌트로.
```

폴더가 없으면 Claude가 폴더도 함께 생성합니다.

---

## 파일 수정

기존 파일을 수정할 수 있습니다.

### 예시

```
> @index.html에서 제목을 "내 웹사이트"로 바꿔줘
```

```
> @app.js에 console.log 추가해줘
```

### 수정 내용 확인

Claude가 수정하기 전에 변경 내용을 보여줍니다:

```diff
- <h1>Hello World</h1>
+ <h1>내 웹사이트</h1>
```

빨간색(-)은 삭제되는 부분, 초록색(+)은 추가되는 부분입니다.

---

## 파일 내용 보기

```
> @config.json 내용 보여줘
```

```
> @README.md 읽어줘
```

Claude가 파일 내용을 읽고 표시합니다.

**긴 파일을 볼 때:**

```
> @README.md 핵심 내용만 요약해줘
> @config.json에서 port 설정만 찾아줘
```

원하는 정보를 구체적으로 요청하면 더 빠르게 찾을 수 있습니다.

---

## 여러 파일 처리

### 여러 파일 동시 참조

```
> @index.html과 @style.css 보고 스타일이 제대로 적용되는지 확인해줘
```

```
> @src/api.js랑 @src/utils.js 비교해서 중복 코드 찾아줘
```

### 파일 패턴으로 검색

```
> src 폴더에서 .js 파일 다 찾아줘
```

```
> 테스트 파일들 목록 보여줘
```

---

## 특수 접두사

`@` 외에도 유용한 접두사들이 있습니다.

| 접두사 | 기능 | 예시 |
|--------|------|------|
| `@` | 파일/폴더 참조 | `@src/app.js` |
| `!` | 명령어 실행 후 결과 표시 | `!ls` |
| `#` | Claude 메모리에 저장 | `# 항상 한국어로 대답해` |

### ! 사용 예시

```
> !ls
```
현재 폴더의 파일 목록을 표시하고, Claude가 그 내용을 인식합니다.

```
> !cat package.json
```
파일 내용을 터미널에 출력하고, Claude가 그 내용을 인식합니다.

### # 사용 예시

```
> # 이 프로젝트는 TypeScript를 사용해
```
Claude가 이 규칙을 기억합니다. 저장 위치를 확인합니다.

---

## 실습

### 1. 파일 생성

```
> my-first-page.html 만들어줘.
> 제목은 "나의 첫 웹페이지", 본문에는 자기소개 넣어줘.
```

### 2. 파일 수정

```
> @my-first-page.html 배경색을 하늘색으로 바꿔줘
```

### 3. 여러 파일 생성

```
> my-project 폴더 만들고 그 안에 index.html, style.css, script.js 만들어줘.
> 간단한 "Hello World" 웹페이지로.
```

### 4. 파일 비교

```
> @index.html이랑 @about.html 구조 비교해줘
```

---

## 미니 프로젝트: 자기소개 웹페이지 ⭐

이번 챕터에서 학습한 내용으로 자기소개 페이지를 만들어봅니다.

### 목표

- HTML/CSS 기초 익히기
- 파일 생성과 수정 연습

### 생성

```
> 자기소개 페이지 만들어줘.
> - 이름과 프로필 사진 자리
> - 간단한 소개 문단
> - 좋아하는 것 목록
> - 예쁜 스타일 적용
```

### 개선

```
> 배경색을 파스텔톤으로 바꿔줘
```
→ CSS 색상 시스템 (`hex`, `rgb`, `hsl`) 익히기

```
> 글씨체를 더 예쁘게 바꿔줘
```
→ 웹폰트와 `font-family` 스택 이해

```
> 호버하면 색이 바뀌는 효과 추가해줘
```
→ CSS 가상 클래스로 인터랙션 구현

### 도전 과제

```
> 다크 모드 버전도 만들어줘
```
CSS 변수 기반 테마 시스템 - 현대 웹의 필수 기능

```
> 여러 페이지로 나눠줘 (index.html, about.html, contact.html)
```
멀티 페이지 구조와 내비게이션 설계

---

## 따라해보세요

파일 작업을 단계별로 연습해봅시다.

### 연습 1: 첫 파일 만들기
```
> my-notes.txt 파일 만들어줘. 안에 "Claude Code 배우기 재밌다!" 써줘.
```
Claude가 만든 후, 확인해보십시오:
```
> @my-notes.txt 안에 뭐가 있는지 보여줘
```

### 연습 2: 파일 수정하기
```
> @my-notes.txt에 "이건 AI로 만들었어!"라는 줄 추가해줘
```
Claude가 변경 사항을 적용하기 전에 보여주는 것을 확인하십시오.

### 연습 3: 여러 파일 한 번에 만들기
```
> my-website 폴더 만들고 그 안에 index.html, style.css, script.js 만들어줘.
> "내 사이트에 오신 것을 환영합니다"라고 나오는 간단한 페이지로.
```

### 연습 4: 파일 탐색하기
```
> 이 폴더에 무슨 파일이 있어?
> @my-website/ 구조 설명해줘
```

---

## 안 되면?

**"파일을 찾을 수 없음" 에러가 발생합니까?**
- 파일 이름 철자가 맞는지 확인하십시오
- 자동완성 기능을 사용하십시오 (`@` 입력 후 타이핑 시작)
- 파일이 다른 폴더에 있을 수 있습니다 - 경로를 포함해보십시오: `@folder/file.txt`

**파일이 엉뚱한 곳에 생겼습니까?**
- 전체 경로를 지정하십시오: "`my-folder/file.txt` 만들어줘"
- 현재 위치 확인: "지금 어디야?"

**변경 사항이 안 보입니까?**
- 변경을 승인했는지 확인하십시오 (Allow 클릭)
- Claude에게 파일 내용 다시 보여달라고 하십시오

**분명히 있는 파일인데 Claude가 못 찾습니까?**
- 숨김 폴더(`.`으로 시작)에 있을 수 있습니다
- `!ls -la` 명령으로 모든 파일 보기를 시도하십시오

**잘못된 파일을 만들었습니까?**
- Claude에게 삭제 요청: "방금 만든 파일 삭제해줘"
- 또는 파일 탐색기에서 직접 삭제하십시오

---

## 자주 하는 실수

### 파일이 없다고 할 때

```
> @없는파일.txt 보여줘
```
→ 파일 이름을 정확히 입력했는지 확인하십시오. 자동완성을 사용하면 실수를 줄일 수 있습니다.

### 경로가 틀렸을 때

```
> @app.js 보여줘
```
→ 파일이 다른 폴더에 있을 수 있습니다. `@src/app.js`처럼 경로를 포함해보십시오.

### 승인을 안 했을 때

파일이 생성되지 않았다면 승인 요청에서 `Allow`를 선택했는지 확인하십시오.

---

## 정리

이번 챕터에서 학습한 것:
- [x] `@`로 파일 참조하기
- [x] 파일 생성
- [x] 파일 수정
- [x] 여러 파일 처리
- [x] 특수 접두사 (`@`, `!`, `#`)

다음 챕터에서는 터미널 명령어를 다루는 방법을 학습합니다.

[Chapter 05: 터미널 명령어](../Chapter05/README.ko.md)로 진행하십시오.
