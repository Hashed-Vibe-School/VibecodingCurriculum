# Chapter 00: 시작하기

**한국어** | [English](./README.md)

## Prerequisites

이 Chapter를 시작하기 전에 다음을 확인하세요:
- [ ] Node.js (v18+)가 설치되어 있나요?
- [ ] 터미널/커맨드 라인 사용에 익숙한가요?
- [ ] 코드 에디터가 있나요? (VS Code 권장)

---

## Introduction

Claude Code는 AI-assisted 개발을 터미널에서 직접 사용할 수 있게 해주는 CLI(Command Line Interface) 도구입니다. 웹 기반 채팅 인터페이스와 달리, Claude Code는 로컬 환경에서 직접 코드를 읽고, 쓰고, 실행할 수 있습니다.

### Claude.ai와 Claude Code의 차이

| Claude.ai (웹) | Claude Code (CLI) |
|----------------|-------------------|
| 브라우저에서 실행 | 터미널에서 실행 |
| 코드 복사-붙여넣기 필요 | 파일 직접 읽기/쓰기 |
| 대화 컨텍스트만 사용 | 전체 프로젝트 구조 인식 |
| 수동 코드 적용 | 자동 코드 편집 및 실행 |

### 왜 Claude Code인가?

- **직접 파일 접근**: 복사-붙여넣기 없이 파일 읽기/수정. `@파일명`으로 파일을 참조하면 Claude가 직접 읽습니다.
- **컨텍스트 인식**: 전체 프로젝트 구조를 이해하고, 관련 파일들 간의 의존성을 파악합니다.
- **도구 통합**: `git commit`, `npm install`, 테스트 실행 등 터미널 명령어를 직접 실행합니다.
- **프라이버시**: 코드는 로컬에 유지됩니다. 프롬프트와 필요한 컨텍스트만 API로 전송됩니다.

---

## Topics

### 1. 설치

```bash
# 네이티브 바이너리 (권장 - 가장 빠름)
curl -fsSL https://claude.ai/install.sh | bash

# 또는 npm으로 설치
npm install -g @anthropic-ai/claude-code
```

> **참고**: 네이티브 바이너리는 더 나은 성능과 자동 업데이트를 위해 권장됩니다.

### 2. 인증

```bash
# Claude Code 시작 후 인증 프롬프트 따라하기
claude

# 또는 API 키 직접 사용
export ANTHROPIC_API_KEY="your-api-key"
claude
```

### 3. 기본 명령어

| 명령어 | 설명 |
|--------|------|
| `claude` | 대화형 세션 시작 |
| `claude "prompt"` | 일회성 명령 |
| `claude -p "prompt"` | 출력 모드 (비대화형) |
| `/help` | 사용 가능한 모든 명령어 표시 |
| `/clear` | 대화 기록 삭제 |
| `/exit` 또는 `Ctrl+C` | Claude Code 종료 |

### 4. 세션 관리

세션을 이어서 작업하세요:

```bash
# 마지막 세션 이어하기
claude --continue

# 과거 세션에서 선택
claude --resume

# 현재 세션에 이름 붙이기
/rename my-feature-work

# 이름으로 세션 재개
claude --resume my-feature-work
```

### 5. 필수 단축키

| 단축키 | 동작 |
|--------|------|
| `Esc Esc` | 이전 체크포인트로 되돌리기 |
| `Shift+Tab` 또는 `Alt+M` | 권한 모드 전환 (Normal → Accept Edits → Plan) |
| `Alt+P` / `Option+P` | 세션 중 모델 전환 |
| `Ctrl+C` | 현재 작업 취소 |
| `Ctrl+L` | 터미널 화면 지우기 (대화 기록 유지) |
| `Ctrl+B` | 현재 작업 백그라운드로 이동 |
| `Shift+Enter` | 입력에서 줄바꿈 |

> **참고**: macOS에서 Alt 키 조합을 사용하려면 터미널 설정에서 "Option as Meta"를 활성화해야 합니다.

### 6. 권한 모드 (중요!)

Claude Code는 세 가지 권한 모드로 어떤 작업에 승인이 필요한지 제어합니다. **이 개념을 잘 이해하는 것이 Claude Code를 효과적으로 사용하는 핵심입니다.**

#### Plan Mode (계획 모드)

```
📋 Plan Mode - 읽기 전용
파일 읽기와 분석만 가능. 수정, 생성, 명령어 실행 모두 차단.
```

**언제 사용?**
- 새 프로젝트/코드베이스를 처음 탐색할 때
- 큰 변경을 하기 전에 계획을 세울 때
- 코드를 이해하고 싶을 때 (수정 없이)

#### Normal Mode (기본 모드)

```
🔒 Normal Mode - 매번 확인
모든 파일 편집, 명령어 실행 전에 승인 요청.
```

**언제 사용?**
- Claude Code를 처음 배울 때
- 중요한 코드를 수정할 때
- 무엇이 변경되는지 하나씩 확인하고 싶을 때

#### Accept Edits Mode (편집 승인 모드)

```
⚡ Accept Edits Mode - 파일 편집 자동 승인
파일 수정은 자동 승인. 위험한 명령어(rm, git push 등)만 확인.
```

**언제 사용?**
- 이미 잘 아는 프로젝트에서 빠르게 작업할 때
- 반복적인 수정 작업을 할 때
- Claude를 충분히 신뢰할 수 있을 때

#### 모드 전환 방법

```bash
# CLI 옵션으로 시작
claude --permission-mode plan
claude --permission-mode acceptEdits

# 세션 중 전환
Shift+Tab  # 모드 순환: Normal → Accept Edits → Plan → Normal
/plan      # Plan 모드로 직접 진입
```

> **Pro Tip**: "거의 항상 Plan 모드로 시작하세요." 새 작업을 시작할 때 `Shift+Tab`을 눌러 Plan 모드로 진입하세요. Claude가 코드를 먼저 이해하고 계획을 세운 후, Normal이나 Accept Edits로 전환하면 더 나은 결과를 얻습니다.

### 7. 첫 번째 상호작용

```bash
# Claude Code 시작
claude

# 이 프롬프트들을 시도해보세요:
> What files are in this directory?
> Explain what this project does
> Create a simple hello.py file
```

---

## Resources

- [Claude Code 공식 문서](https://code.claude.com/docs)
- [Quick Start Guide](https://code.claude.com/docs/en/quickstart)
- [GitHub 저장소](https://github.com/anthropics/claude-code)
- [Anthropic API 가격](https://www.anthropic.com/pricing)

---

## Checklist

면접에서 답변하듯이 다음 질문에 답해보세요:

1. **Claude Code란 무엇이며, ChatGPT나 Claude.ai와 어떻게 다른가요?**
   <details>
   <summary>힌트</summary>
   생각해볼 것: 로컬 파일 접근, CLI vs 웹, 컨텍스트 인식, 도구 실행
   </details>

2. **세 가지 권한 모드는 무엇이고, 각각 언제 사용하나요?**
   <details>
   <summary>힌트</summary>
   Normal, Accept Edits, Plan Mode - 신뢰 수준과 작업 유형에 따라 선택
   </details>

3. **Claude Code는 코드 프라이버시를 어떻게 처리하나요?**
   <details>
   <summary>힌트</summary>
   무엇이 API로 전송되나요? 무엇이 로컬에 남나요?
   </details>

4. **`claude "prompt"`와 `claude -p "prompt"`의 차이는 무엇인가요?**
   <details>
   <summary>힌트</summary>
   대화형 vs 출력 모드, 출력 형식 차이
   </details>

---

## Mini Project

### 학습 목표

이 Chapter를 마스터하기 위해 다음 작업을 완료하세요:

- [ ] 시스템에 Claude Code 설치
- [ ] Claude Code 세션 시작 및 기본 명령어 탐색
- [ ] Claude를 사용하여 간단한 "Hello World" 프로그램 생성
- [ ] 사용자 입력을 받도록 프로그램 수정
- [ ] 각 권한 모드 시도 (Default, Accept Edits, Plan)
- [ ] Plan Mode로 오픈소스 프로젝트 탐색
- [ ] 세션을 닫고 다시 열 때 무슨 일이 일어나는지 이해

### 이런 프롬프트를 시도해보세요

```bash
> What can you help me with?
> Create a Python script that greets users by name
> Explain how permission modes work
> Explore the Express.js repository and explain its structure
```

---

## Advanced

### 모델 변경

기본 모델(Opus 4.5)로 대부분의 작업을 처리할 수 있습니다. 필요 시 모델 변경:

```bash
claude --model claude-sonnet-4-5-20250929  # 빠른 응답 필요 시
claude --model claude-haiku                 # 간단한 작업 (린트 등)
```

### Claude 설정 디렉토리 탐색

`~/.claude/` 디렉토리에는 무엇이 있는지 확인해보세요:

```bash
ls -la ~/.claude/
# settings.json  - 전역 설정
# CLAUDE.md      - 개인 전역 메모리
# projects/      - 프로젝트별 설정

# 설정 파일 내용 확인
cat ~/.claude/settings.json
```

### 세션 연속성 활용

작업을 중단했다가 이어서 할 때:

```bash
# 어제 하던 작업 이어하기
claude --continue

# 특정 세션 선택해서 재개
claude --resume

# 다른 컴퓨터에서 세션 이어하기
claude --teleport
# (QR 코드 또는 코드로 세션 공유)
```

### IDE 확장 (선택사항)

터미널 외에도 VS Code, Cursor, Zed 등의 IDE에서 Claude Code를 사용할 수 있습니다. 확장 마켓플레이스에서 "Claude Code"를 검색하세요.

IDE 확장은 시각적 diff와 파일 참조에 편리하지만, Claude Code의 핵심 기능은 모두 터미널에서 사용 가능합니다.

### Pro Tips

> **"Claude에게 작업을 검증할 방법을 제공하세요"** - 가장 중요한 팁입니다.

```bash
# 나쁜 예: 검증 없이 끝
> Add input validation to the form

# 좋은 예: 검증 포함
> Add input validation to the form, then run the tests to make sure it works
```

코드 작성 후 항상 검증을 요청하면 품질이 극적으로 향상됩니다.
